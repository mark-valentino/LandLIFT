import {StyleSheet} from 'react-native';

export const global = StyleSheet.create({
  wideTextInput: {
    width: '100%',
    borderWidth: 1,
    borderColor: '#D9D9D9',
    borderRadius: 4,
  },
  inputLabel: {
    color: '#000',
  },
});
